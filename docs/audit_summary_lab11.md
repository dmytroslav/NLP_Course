# Audit Summary: Lab 11

1. **Extraction-кейс:** Військові інциденти (Military Event Extraction).
2. **Evaluation set:** 15 текстів (використано adversarial/стрес-тест набір для виявлення реальних меж моделі).
3. **Raw valid JSON rate:** 66.7% (10/15).
4. **Post-repair valid JSON rate:** 73.3% (11/15).
5. **Schema-valid JSON rate:** 73.3% (усі успішні JSON суворо відповідають схемі).
6. **Найчастіше ламалися поля:** `event_type` (порушення `enum`), `weapons` (пропуск обов'язкового масиву).
7. **Наймасовіші помилки:** `missing required field` та `schema violation`.
8. **Чи спрацював schema-first підхід:** Так, ідеально. Замість того, щоб пропускати галюцинації моделі далі по пайплайну, валідатор блокував невірні типи даних (наприклад, "засідка" замість дозволених значень), захищаючи цілісність даних.