# Dataset Card: Ukrainian Fake News Classification

## Project
Класифікація українських новин на правдиві та фейкові (пропагандистські).

## Task
[cite_start]**Type:** A. Text Classification (Binary) [cite: 6]
**Input:** Текст заголовка новини (Headline).
**Output:** Мітка класу (True / Fake).

## Data Source
**Original Source:** Kaggle "Ukrainian fake and true news" (Dataset 4).
**URL:** https://www.kaggle.com/datasets/zepopo/ukrainian-fake-and-true-news/data
**Origin:** Дані зібрані з Telegram-каналів (Суспільне, Perepichka, та проросійські фейкові канали).

## [cite_start]Volume [cite: 62]
- **Кількість текстів:** ~10,700
- **Кількість класів:** 2 (True, Fake)
- **Розподіл:** ~8237 (True) vs ~2498 (Fake).

## [cite_start]Language & Domain [cite: 63]
- **Мова:** Українська (UA).
- **Домен:** Новини воєнного часу, політика, соціум (2022 рік).

## [cite_start]Cleaning Process [cite: 64]
1. Видалення зайвих пробілів та переносів рядків.
2. Уніфікація апострофів (всі варіанти зведені до `'`).
3. Заміна посилань на токен `<URL>` та email-адрес на `<EMAIL>`.
4. Видалення (або маркування) точних дублікатів.

## [cite_start]Risks [cite: 65]
1. **Дисбаланс класів:** Правдивих новин у 3 рази більше, ніж фейкових. Модель може схилятися до передбачення мажоритарного класу.
2. **Зміщення джерела (Source Bias):** Оскільки фейки бралися зі специфічних каналів, модель може вивчити стиль цих каналів, а не саму суть неправдивості.
3. **Шум:** Наявність дуже коротких заголовків (менше 5 слів).

## [cite_start]Next Steps [cite: 66]
У Lab 2 планується:
- Провести токенізацію та лематизацію.
- Вирішити проблему дисбалансу (ваги класів або resampling).
- Видалити занадто короткі тексти, якщо вони не несуть змісту.




## Оновлення (Lab 2: Preprocessing)
**Що зроблено:**
- Впроваджено детермінований пайплайн очистки тексту.
- Виконано нормалізацію апострофів, лапок, тире.
- Замасковано PII (URL, Email, Phone).
- Реалізовано sentence splitting за допомогою `spaCy`, що коректно обробляє українські скорочення.

**Ризики (зняті та залишені):**
- *Знято:* Ризик шуму від HTML-артефактів та різного форматування спецсимволів (знято завдяки нормалізації).
- *Знято:* Ризик втрати контексту при агресивному розбитті речень на скороченнях.
- *Залишились:* Дисбаланс класів (True значно більше за Fake).
- *Залишились:* Можлива втрата специфічного сленгу, якщо він був написаний з навмисними помилками (пропагандистський сленг), хоча базова чистка намагається його не чіпати.




## Оновлення (Lab 3: Lemma & POS)
**Що зроблено:**
- Згенеровано `lemma_text` та `pos_seq` за допомогою spaCy.
- Побудовано базові ML-моделі (Logistic Regression + TF-IDF) для перевірки гіпотези, чи покращують леми класифікацію українських фейкових новин.


## Updates for Lab 4 (Information Extraction)
- **Витягнуті поля:** DATE (дати), AMOUNT (грошові суми), LOCATION (міста/регіони).
- **Ризики / PII:** Витягування LOCATION може опосередковано розкривати геолокацію певних подій, але для новин це публічна інформація. Грошові суми (AMOUNT) не прив'язані до конкретних осіб (PII). Раніше замасковані `<URL>`, `<EMAIL>`, `<PHONE>` залишаються анонімізованими.



## Splits & Leakage (Lab 5)
- **Розбиття**: Stratified random split (80/10/10) по колонці `label`.
- **Баланс**: Співвідношення класів 76.7% / 23.3% ідеально збережено у всіх вибірках.
- **Виявлені ризики**: Знайдено незначний leakage (22 точні дублікати між train та test, 23 між train та val), які потребують очищення перед навчанням.


## Аналіз особливостей даних (за результатами ЛР7)
- **Дисбаланс класів (Imbalance):** Наявний (76.7% True / 23.3% False). Використання зважених класів є обов'язковим для уникнення деградації міноритарного класу.
- **Шум у тексті (Noisy text):** Дані містять значну кількість нестандартної лексики, трансліту та специфічного форматування (особливо з Telegram). Використання символьних n-грам (char-ngrams) довело свою ефективність, значно покращивши загальні метрики.
- **Найпроблемніший клас:** False (Фейки), оскільки вони часто мімікрують під структуру офіційних повідомлень (клас True).
- **Threshold логіка:** Дефолтний поріг рішення балансує результати. Однак для продуктового рішення (наприклад, фактчекінг-бота) доцільним є компроміс у бік вищого Recall для класу False (зсув порогу), щоб мінімізувати кількість пропущених маніпуляцій.


## Тематична структура корпусу (за результатами ЛР8)
* **Виявлені теми:** Корпус має виражені сюжетні кластери, типові для українського інфопростору під час війни: зведення про обстріли, повітряні тривоги, міжнародна допомога та санкції.
* **Однорідність:** Корпус містить велику кількість шаблонних повідомлень (наприклад, про тривоги), але загалом теми досить змішані через специфіку Telegram-каналів.
* **Шум:** Виявлено значний вплив стоп-слів (прийменники, сполучники) та загальної новинної лексики ("повідомляють", "канали", "змі"), що утворюють "погані" теми.
* **Корисність Topic Modeling:** Інструмент корисний для розуміння макро-сюжетів датасету, але для точного кластеризування вимагає агресивнішого очищення даних від стоп-слів та переходу до лематизації.


## NER Lab 10 Update: Entity Recognition
* **Важливі типи сутностей:** У цьому домені (військові новини) найважливішими є `LOC`, `ORG` (військові та державні інституції), `PER`, `WEAPON` (назви зброї та техніки), `DATE` та `MONEY`.
* **Чи достатній стандартний NER pipeline:** Ні. Стандартний spaCy (`uk_core_news_sm`) непогано знаходить базові топоніми, але критично провалюється на доменній лексиці та специфічних форматах дат.
* **Проблемні доменні сутності:** Специфічні українські абревіатури (ППО, ЗСУ), назви озброєння латиницею (HIMARS, Storm Shadow) та вказівки точного часу (Вчора о 15:00).
* **Виграш від hybrid rules:** Додавання `EntityRuler` та `Regex` дало відчутний виграш — ми змогли швидко покрити специфічний словник та патерни чисел, не перенавчаючи модель (zero-shot покращення).
* **Які типи помилок залишаються:** Основні проблеми зараз — це *false positives* від занадто агресивних регулярних виразів та *boundary errors* на багатослівних сутностях.


## LLM Extraction (Lab 11 Update)
* **Тип structured extraction:** Schema-first extraction у формат JSON із використанням суворої валідації (`jsonschema`).
* **Особливості корпусу:** Корпус містить як явні значення (назви міст, типи ракет), так і неявні (відсутність зброї або локації, що вимагає коректної обробки `null`).
* **Складні випадки:** Найбільшу складність для LLM становлять нерелевантні тексти (наприклад, побутові питання) або надто короткі фрази. Модель схильна галюцинувати обов'язкові поля, щоб задовольнити схему.
* **Життєздатність LLM extraction:** Підхід є життєздатним (Raw Valid Rate: 66.7%, Post-repair: 73.3% на складному стрес-тесті). 
* **Стабільність:** Schema-first pipeline забезпечив повний контроль над виводом. Ми гарантовано відфільтровуємо галюцинації моделі завдяки валідатору.